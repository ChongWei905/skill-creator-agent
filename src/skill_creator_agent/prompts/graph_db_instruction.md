## Optional Graph Guidance

Graph access is optional for this agent. If graph tools are available and the requested skill depends on graph data:

1. Inspect entity types first.
2. Inspect relationships second.
3. Pull schema details only for the entities the skill really needs.
4. Do not block the rest of the workflow when graph access is unavailable.
